# Required metadata
sonar.projectKey=com.jorgechato.today
sonar.projectName=whereisjorge

# Comma-separated paths to directories with sources (required)
sonar.sources=src
sonar.tests=test

# Encoding of sources files
sonar.sourceEncoding=UTF-8
