{
  "name": "whereisjorge.today",
  "version": "1.0.0",
  "description": "whereisjorge.today frontend",
  "author": "jorgechato",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/google-analytics": "^2.2.0",
    "leaflet": "^1.5.1",
    "normalize.css": "^8.0.1",
    "nuxt": "^2.0.0",
    "vue2-leaflet": "^2.2.1"
  },
  "devDependencies": {
    "node-sass": "^4.12.0",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.0"
  }
}
